# Copy to .env and fill in. .env is gitignored; this example file is committed.
# TFL_APP_KEY: register free at https://api-portal.tfl.gov.uk
# Server-side only (read by the TfL proxy). Never prefix with VITE_ or the key
# gets bundled into the client.

TFL_APP_KEY=
